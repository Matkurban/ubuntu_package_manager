import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('app launches smoke test', (WidgetTester tester) async {
    // Smoke test placeholder — full integration tests require system apt/dpkg.
    expect(true, isTrue);
  });
}
